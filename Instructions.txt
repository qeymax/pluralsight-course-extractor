1 - Make sure both "Input" & "Output" are empty.
2 - Copy SORTED Course files into the "Input" folder.
3 - If first use run the "npm install" command before running the application.
5- Run the app by using "node app.js" command in your terminal.
